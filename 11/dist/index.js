"use strict";
// never type
function sum(a, b) {
    // return a + b;
    let c = a + b; //void
}
function raiseError(message) {
    throw new Error(message);
}
function forever() {
    while (true) {
        console.log("typescript");
    }
}
